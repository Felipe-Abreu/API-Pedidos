##PostgreSQL
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/apiPedidos"
    username: "postgres"
    password: "postgres"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
